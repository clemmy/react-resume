import React, { PropTypes } from 'react';

const Header = () => (
  <section style={style}> + RESUME</section>
);

const style = {
  padding: '6px 0 6px 24px',
  color: 'white',
  flex: '1 1 auto',
  fontSize: '1.6rem',
  background: 'rgba(47, 152, 175, 1)',
};

export default Header;
